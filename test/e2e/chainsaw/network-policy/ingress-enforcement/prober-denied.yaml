apiVersion: v1
kind: Pod
metadata:
  name: prober-denied
  labels:
    network-policy-test: denied
spec:
  restartPolicy: Never
  terminationGracePeriodSeconds: 1
  containers:
    - name: prober
      image: busybox:1.36
      command: ["sleep", "3600"]
