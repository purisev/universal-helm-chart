{{- $ctx := . }}
{{- $releaseName := include "uhc.fullname" . -}}
{{- range $wlName := keys $ctx.Values.deployments | sortAlpha }}
{{- $wl := index $ctx.Values.deployments $wlName }}
{{- if and (hasKey $wl "enabled") $wl.enabled }}
{{- if and $wl.networkPolicy $wl.networkPolicy.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $releaseName }}-{{ $wlName }}
  labels:
    {{- include "uhc.workloadLabels" (dict "ctx" $ctx "workloadName" $wlName) | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "uhc.workloadSelectorLabels" (dict "ctx" $ctx "workloadName" $wlName) | nindent 6 }}
  policyTypes:
    {{- toYaml ($wl.networkPolicy.policyTypes | default (list "Ingress")) | nindent 4 }}
  {{- with $wl.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $wl.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- range $wlName := keys ($ctx.Values.statefulSets | default dict) | sortAlpha }}
{{- $wl := index $ctx.Values.statefulSets $wlName }}
{{- if and (hasKey $wl "enabled") $wl.enabled }}
{{- if and $wl.networkPolicy $wl.networkPolicy.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $releaseName }}-{{ $wlName }}
  labels:
    {{- include "uhc.workloadLabels" (dict "ctx" $ctx "workloadName" $wlName) | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "uhc.workloadSelectorLabels" (dict "ctx" $ctx "workloadName" $wlName) | nindent 6 }}
  policyTypes:
    {{- toYaml ($wl.networkPolicy.policyTypes | default (list "Ingress")) | nindent 4 }}
  {{- with $wl.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $wl.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
