{{- if .Values.initJob.enabled }}
{{- $ctx := . }}
{{- $initJobSpec := $ctx.Values.initJob }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "uhc.fullname" . }}-init
  annotations:
    {{- $baseAnnotations := dict
      "argocd.argoproj.io/hook" "Sync"
      "argocd.argoproj.io/sync-wave" (printf "%d" (add 1 (int .Values.syncWaves.jobsSyncWave)))
    }}
    {{- $finalAnnotations := mergeOverwrite $baseAnnotations ($initJobSpec.metadataAnnotations | default dict) }}
    {{- toYaml $finalAnnotations | nindent 4 }}
  labels:
    {{- include "uhc.labels" . | nindent 4 }}
spec:
  backoffLimit: {{ default 3 $initJobSpec.backoffLimit }}
  {{- with $initJobSpec.ttlSecondsAfterFinished }}
  ttlSecondsAfterFinished: {{ . | int }}
  {{- end }}
  {{- with $initJobSpec.activeDeadlineSeconds }}
  activeDeadlineSeconds: {{ . | int }}
  {{- end }}
  template:
    metadata:
    {{- $jobAnnotations := merge ($ctx.Values.jobPodAnnotations | default dict) ($ctx.Values.podAnnotations | default dict) }}
    {{- with $jobAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      name: {{ .Release.Name }}
      labels:
        {{- include "uhc.initJobLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "uhc.serviceAccountName" . }}
      automountServiceAccountToken: {{ $ctx.Values.automountServiceAccountToken | default false }}
      securityContext:
        {{- toYaml $ctx.Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ $ctx.Values.terminationGracePeriodSeconds | default 30 | int }}
      restartPolicy: Never
      initContainers:
        {{- range $taskName := keys $initJobSpec.tasks | sortAlpha }}
        {{- $task := index $initJobSpec.tasks $taskName }}
        {{- if $task.enabled }}
        - name: {{ $taskName }}
          image: "{{ $ctx.Values.image.repository }}:{{ $ctx.Values.image.tag }}"
          imagePullPolicy: {{ $ctx.Values.image.pullPolicy }}
          securityContext:
            {{- toYaml $ctx.Values.securityContext | nindent 12 }}
          {{- if $task.command }}
          command:
            - sh
            - -c
            - |
{{ $task.command | trim | indent 14 }}
          {{- else if $task.args }}
          args:
            {{- range $task.args }}
            - {{ . | quote }}
            {{- end }}
          {{- end }}
          env:
            {{- include "uhc.envVars" (dict "ctx" $ctx "extraEnv" $initJobSpec.env) | nindent 12 }}
          {{- include "uhc.envFrom" (dict "rootSecrets" $ctx.Values.envSecrets) | nindent 10 }}
          {{- with (index $ctx.Values.volumeMounts "initJob") }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- end }}
        {{- end }}
      containers:
        - name: job-completed
          image: {{ $initJobSpec.completionImage | default "alpine:3.21" }}
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "echo SUCCESS"]
          securityContext:
            {{- toYaml $ctx.Values.securityContext | nindent 12 }}
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              memory: 16Mi
      volumes:
        {{- with $ctx.Values.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
