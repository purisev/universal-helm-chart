ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    myapp.example.com:
      paths:
        - path: /
          pathType: Prefix
          servicePort: 8080
  tls:
    myapp-tls:
      hosts:
        - myapp.example.com
