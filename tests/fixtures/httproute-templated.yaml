httpRoute:
  enabled: true
  parentRefs:
    - name: "{{ .Release.Name }}-gateway"
      namespace: "{{ .Release.Namespace }}"
  hostnames:
    - "{{ .Release.Namespace }}.example.com"
  rules:
    - backendRefs:
        - name: myapp
          port: 80
