integrations:
  eso:
    externalSecrets:
      cfg:
        enabled: true
        secretStore: vault-store
        dataFrom:
          - remoteKey: my-app/config
            decodingStrategy: Base64
            metadataPolicy: Fetch
