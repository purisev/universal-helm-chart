integrations:
  eso:
    externalSecrets:
      mixed:
        enabled: true
        secretStore: vault-store
        data:
          DB_URL:
            remoteKey: my-app/db
            property: url
        dataFrom:
          - remoteKey: my-app/config
