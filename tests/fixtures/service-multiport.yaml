image:
  repository: nginx
  tag: v1.0.0
deployments:
  web:
    enabled: true
    service:
      enabled: true
      type: ClusterIP
      ports:
        http:
          port: 80
          targetPort: 8080
        metrics:
          port: 9090
          targetPort: 9090
          protocol: TCP
