image:
  repository: nginx
  tag: v1.0.0
statefulSets:
  cache:
    enabled: true
    keda:
      enabled: true
      minReplicas: 1
      maxReplicas: 4
      triggers:
        - type: cpu
          metricType: Utilization
          metadata:
            value: "60"
