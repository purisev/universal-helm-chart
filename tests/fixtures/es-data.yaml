integrations:
  eso:
    externalSecrets:
      db-creds:
        enabled: true
        secretStore: vault-store
        data:
          DB_PASSWORD:
            remoteKey: my-app/db
            property: password
